/** @type {import('ts-jest').JestConfigWithTsJest} */
module.exports = {
  // ts-jest fails when function signatures change (expecting more/less arguments) making TDD hard
  // preset: 'ts-jest',
  // testEnvironment: 'node',
};
